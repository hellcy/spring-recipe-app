spring:
  datasource:
    url: jdbc:mysql://localhost:3306/yuan_prod
    username: yuan_prod_user
    password: 1
    initialization-mode: always
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false